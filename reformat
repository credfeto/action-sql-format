#! /bin/sh

BASEDIR="$(dirname "$(readlink -f "$0")")"

echo "BASEDIR: $BASEDIR"
ls -la "$BASEDIR"

ls -la /usr/bin/py*

echo "Reformatter..."
ls -la /app/reformat.py
echo "^^^^^^"

set -eu
find ./ -iname '*.sql' -type f -exec bash -c '/app/reformat.py "$0"' {} \;
echo "Done"
